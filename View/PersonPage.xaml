<Page
    x:Class="MyInsta.View.PersonPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyInsta.View"
    xmlns:localInsta="using:InstagramApiSharp.Classes.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xml:space="preserve"
    xmlns:model="using:MyInsta.Model"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Unloaded="Page_Unloaded">
    <Page.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Century Gothic"/>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="34"/>
            <RowDefinition Height="166"/>
            <RowDefinition Height="46"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="131*"/>
            <ColumnDefinition Width="131*"/>
            <ColumnDefinition Width="131*"/>
            <ColumnDefinition Width="131*"/>
        </Grid.ColumnDefinitions>
        
        <Ellipse  Grid.Column="0" Grid.Row="0" Margin="20,20,20,20" Grid.RowSpan="3" Height="200" Width="200">
            <Ellipse.Fill>
                <ImageBrush>
                    <ImageBrush.ImageSource>
                        <BitmapImage UriSource="{x:Bind InstaUserInfo.ProfilePicUrl}"/>
                    </ImageBrush.ImageSource>
                </ImageBrush>
            </Ellipse.Fill>
        </Ellipse>
        
        
        <ScrollViewer Grid.Row="3">
            <StackPanel  Margin="10, 0, 10, 0">
            <Button Grid.Row="1" Grid.Column="0" Content="Action" CornerRadius="5" HorizontalAlignment="Stretch" Margin="10">
                <Button.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutItem x:Name="followButton" CornerRadius="5" IsEnabled="{x:Bind ButtonFollow, Mode=TwoWay}" Click="FollowButton_Click" HorizontalAlignment="Stretch" Text="Follow"/>
                        <MenuFlyoutItem x:Name="unfollowButton" CornerRadius="5" IsEnabled="{x:Bind ButtonUnFollow, Mode=TwoWay}" Click="UnfollowButton_Click" HorizontalAlignment="Stretch" Text="Unfollow"/>
                        <MenuFlyoutItem x:Name="saveButton" CornerRadius="5" Click="SaveButton_Click" HorizontalAlignment="Stretch" Text="Download posts"/>
                        <MenuFlyoutItem x:Name="unlikeButton" CornerRadius="5"  Click="UnlikeButton_Click"  HorizontalAlignment="Stretch" Text="Unlike 30 posts"/>
                        <MenuFlyoutSubItem Text="Bookmark">
                            <MenuFlyoutItem x:Name="bookMarksButton" CornerRadius="5"  Click="BookMarksButton_Click"  HorizontalAlignment="Stretch" Text="Add to bookmarks"/>
                            <MenuFlyoutItem x:Name="d_bookMarksButton" CornerRadius="5"  Click="D_bookMarksButton_Click"  HorizontalAlignment="Stretch" Text="Remove from bookmarks"/>
                        </MenuFlyoutSubItem>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
            <TextBlock Text="{x:Bind InstaUserInfo.FullName}"  
                        Grid.Column="1" FontSize="25" TextWrapping="Wrap" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" Margin="5, 30, 5, 5">
                <TextBlock x:Uid="Following" FontSize="20" Margin="0, 0, 10, 0"/>
                <TextBlock Text="{x:Bind InstaUserInfo.FollowingCount}"  
                        Grid.Column="1" FontSize="20" TextWrapping="Wrap"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <TextBlock x:Uid="Followers2" FontSize="20" Margin="0, 0, 10, 0"/>
                <TextBlock Text="{x:Bind InstaUserInfo.FollowerCount}"  
                        Grid.Column="1" FontSize="20" TextWrapping="Wrap"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <TextBlock x:Uid="Media" FontSize="20" Margin="0, 0, 10, 0"/>
                <TextBlock Text="{x:Bind InstaUserInfo.MediaCount}"  
                        Grid.Column="1" FontSize="20" TextWrapping="Wrap"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <TextBlock x:Uid="IsPrivate" FontSize="20" Margin="0, 0, 10, 0"/>
                <TextBlock Text="{x:Bind InstaUserInfo.IsPrivate}"  
                        Grid.Column="1" FontSize="20" TextWrapping="Wrap"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <TextBlock x:Uid="FollowingOn" FontSize="20" Margin="0, 0, 10, 0"/>
                <TextBlock Text="{x:Bind InstaUserInfo.FriendshipStatus.Following}"  
                         FontSize="20"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <TextBlock x:Uid="FollowedBy" FontSize="20" Margin="0, 0, 10, 0"/>
                <TextBlock Text="{x:Bind InstaUserInfo.FriendshipStatus.FollowedBy}"  
                         FontSize="20" />
            </StackPanel>
        </StackPanel>
        </ScrollViewer>
        
        <controls:TabView Grid.Column="1" Grid.ColumnSpan="4" Grid.Row="0" Grid.RowSpan="4" TabWidthBehavior="Equal" Margin="10">
            <controls:TabViewItem x:Uid="Posts" Background="{ThemeResource FlipViewButtonBorderThemeBrush}" Icon="Camera">     
                <Frame Margin="10" CornerRadius="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="70"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <ProgressRing x:Name="progressPosts" Height="90" Width="90" Grid.Row="1"/>
                        <AutoSuggestBox x:Name="postBox" QuerySubmitted="PostBox_QuerySubmitted" QueryIcon="Find" TextChanged="PostBox_TextChanged" PlaceholderText="Number of post"  Margin="10" Grid.Row="0"/>
                        <ScrollViewer x:Name="scrollListPosts" Grid.Row="1" ViewChanged="ScrollListPosts_ViewChanged" VerticalScrollBarVisibility="Auto">
                            <ListView x:Name="mediaList" SelectionMode="None" ItemsSource="{x:Bind Posts}" HorizontalContentAlignment="Center"> 
                                <ListView.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel HorizontalAlignment="Center"/>
                                    </ItemsPanelTemplate>
                                </ListView.ItemsPanel>
                                <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="model:PostItem">
                                            <StackPanel Orientation="Vertical" > 
                                                <FlipView Width="500" x:Name="itemsList" Tapped="MediaList_SelectionChanged" ItemsSource="{x:Bind Items}" HorizontalContentAlignment="Center">
                                                    <FlipView.ItemsPanel>
                                                        <ItemsPanelTemplate>
                                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                                                        </ItemsPanelTemplate>
                                                    </FlipView.ItemsPanel>
                                                    <FlipView.ItemTemplate>
                                                        <DataTemplate x:DataType="model:CustomMedia">
                                                            <StackPanel>
                                                                <Image Source="{x:Bind UrlSmallImage}" Margin="10" Height="400" />
                                                            </StackPanel>
                                                        </DataTemplate>
                                                    </FlipView.ItemTemplate>
                                                </FlipView>     
                                                <!--<Image Source="{x:Bind }" Margin="10" Height="400" Width="500" HorizontalAlignment="Center"/>-->
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <TextBlock x:Uid="Likes" Margin="5, 5, 10, 5" FontSize="20" FontFamily="Century Gothic"/>
                                                    <TextBlock Text="{x:Bind Items[0].CountLikes}" Margin="5, 5, 10, 5" FontSize="20" FontFamily="Century Gothic"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <TextBlock x:Uid="Comments" Margin="5, 5, 10, 5" FontSize="20" FontFamily="Century Gothic"/>
                                                    <TextBlock Text="{x:Bind Items[0].CountComments}" Margin="5, 5, 10, 5" FontSize="20" FontFamily="Century Gothic"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                    <Button x:Name="buttonSaveInProfile" Tag="{x:Bind Items[0].Pk}" Click="ButtonSaveInProfile_Click" CornerRadius="5" Margin="10">
                                                        <Button.Content>
                                                            <SymbolIcon Symbol="Emoji"/>
                                                        </Button.Content>
                                                    </Button>
                                                    <Button x:Name="buttonDownload" CornerRadius="5" HorizontalAlignment="Center" Click="ButtonDownload_Click" Margin="10" x:Uid="DownloadPost" Tag="{x:Bind Id}"/>
                                                    <Button x:Name="buttonShare" Tag="{x:Bind Id}" Click="ButtonShare_Click" CornerRadius="5" Margin="10">
                                                        <Button.Content>
                                                            <SymbolIcon Symbol="Share"/>
                                                        </Button.Content>
                                                    </Button>
                                                </StackPanel>
                                            </StackPanel>
                                        </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </ScrollViewer>
                    </Grid>
                </Frame>
            </controls:TabViewItem>
            <controls:TabViewItem x:Uid="Stories" Background="{ThemeResource FlipViewButtonBorderThemeBrush}" Icon="View">     
                <Frame Margin="10" CornerRadius="0">
                    <ScrollViewer>
                        <ListView x:Name="storiesList" ItemsSource="{x:Bind UrlStories}" HorizontalContentAlignment="Center" SelectionChanged="StoriesList_SelectionChanged"> 
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel HorizontalAlignment="Center"/>
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="model:CustomMedia">
                                        <StackPanel Orientation="Vertical" >
                                            <Image Source="{x:Bind UrlBigImage}" Margin="10" Height="400" Width="500" HorizontalAlignment="Center"/>
                                            <!--<StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock x:Uid="Likes" Margin="5, 5, 10, 5" FontSize="25" FontFamily="Century Gothic"/>
                                                <TextBlock Text="{x:Bind CountLikes}" Margin="5, 5, 10, 5" FontSize="25" FontFamily="Century Gothic"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <TextBlock x:Uid="Comments" Margin="5, 5, 10, 5" FontSize="25" FontFamily="Century Gothic"/>
                                                <TextBlock Text="{x:Bind CountComments}" Margin="5, 5, 10, 5" FontSize="25" FontFamily="Century Gothic"/>
                                            </StackPanel>-->
                                            <Button x:Name="buttonDownloadStory" CornerRadius="5" HorizontalAlignment="Center" Click="ButtonDownloadStory_Click" Margin="10" x:Uid="DownloadStory" Tag="{x:Bind Name}"/>
                                        </StackPanel>
                                    </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </ScrollViewer>
                </Frame>
            </controls:TabViewItem>
        </controls:TabView>

        
        <!--<Button x:Name="followButton" x:Uid="Follow" CornerRadius="5" IsEnabled="{x:Bind ButtonFollow, Mode=TwoWay}" Click="FollowButton_Click" HorizontalAlignment="Stretch" Margin="5" Grid.Column="1" Grid.Row="0"/>
        <Button x:Name="unfollowButton" CornerRadius="5" IsEnabled="{x:Bind ButtonUnFollow, Mode=TwoWay}" x:Uid="Unfollow" Click="UnfollowButton_Click" Grid.Column="2" Grid.Row="0" HorizontalAlignment="Stretch" Margin="5"/>
        <Button x:Name="saveButton" CornerRadius="5" Click="SaveButton_Click" x:Uid="DownloadMedia" Margin="5" Grid.Column="3" Grid.Row="0" HorizontalAlignment="Stretch"/>
        
        <Button x:Name="unlikeButton" CornerRadius="5"  x:Uid="UnlikeProfile" Click="UnlikeButton_Click"  HorizontalAlignment="Stretch" Margin="5" Grid.Column="4" Grid.Row="0"/>-->
        
    </Grid>
</Page>
